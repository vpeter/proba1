<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <div id="head2">
        <div id="heading">
          névjegykártya online
        </div>
        <div id="nav">
          
          <div>
            <a href="#" target="_self">menfbdfbdfu</a>
          </div>
          <div>
            <a href="#" target="_self">menu</a>
          </div>
          <div class="aktivmenu">
            <a href="#" target="_self">mfbenu</a>
          </div>
          <div>
            <a href="#" target="_self">menfbdf df du</a>
          </div>
          <div>
            <a href="#" target="_self">menu</a>
          </div>
          
        </div>
      </div>
      <div id="content">
          <% flash.each do |name, msg| %>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          <% end %>
          <%= content_tag :h1, yield(:title) if show_title? %>
          <%= yield %>
      </div>
    </div>
  </body>
</html>
