<% title t("order.title_order").capitalize %>

<p>
  <strong>Name:</strong>
  <%= @order.name %>
</p>
<p>
  <strong>Price:</strong>
  <%= @order.price %>
</p>

<p>
  <%= link_to t("order.edit").capitalize, edit_order_path(@order) %> |
  <%= link_to t("order.destroy").capitalize, @order, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to t("order.view_all").capitalize, orders_path %>
</p>
